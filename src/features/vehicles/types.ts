import type { Vehicle, VehicleInsert, VehicleUpdate, VehicleStatusHistory } from '@/types/database';

export type { Vehicle, VehicleInsert, VehicleUpdate, VehicleStatusHistory };

export type { VehicleStatus, FuelType, TransmissionType, SellerType } from '@/lib/constants';
