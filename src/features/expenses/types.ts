import type { Expense, ExpenseInsert, ExpenseUpdate } from '@/types/database';

export type { Expense, ExpenseInsert, ExpenseUpdate };

export type { ExpenseCategory, PaymentStatus } from '@/lib/constants';
